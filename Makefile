cp:
	mkdir -p ./.config/fish      && cp -r ~/.config/fish/*      ./.config/fish
	mkdir -p ./.config/fisher    && cp -r ~/.config/fisher/*    ./.config/fisher
	mkdir -p ./.config/nvim      && cp -r ~/.config/nvim/*      ./.config/nvim
	mkdir -p ./.config/coc       && cp -r ~/.config/coc/*       ./.config/coc
	mkdir -p ./.config/karabiner && cp -r ~/.config/karabiner/* ./.config/karabiner
